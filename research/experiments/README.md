# Experiments

Each experiment must have a unique ID, a hypothesis, a baseline strategy, a
candidate strategy, a dataset identifier and saved metrics. Generated JSON
reports are written here by `ExperimentRunner`.
